@{
    ViewBag.Title = "Danh sách tỉnh";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="page-title">@ViewBag.Title</h2>

<table id="dataTable" class="display">
    <thead>
        <tr>
            <th width="100px;">#</th>
            <th>Tên tỉnh</th>
        </tr>
    </thead>
    <tbody></tbody>
    <tfoot>
        <tr>
            <th>#</th>
            <th>Tên tỉnh</th>
        </tr>
    </tfoot>
</table>

<script>
    $(document).ready(function () {
        $('#dataTable').DataTable({
            serverSide: false,
            ajax: {
                "url": "/Province/GetList/",
                "dataSrc": ""
            },
            columns: [
                { "data": "ProvinceId" },
                { "data": "ProvinceName" },
            ],
            fnPreDrawCallback: function (settings) {
            },
            fnDrawCallback: function (settings) {
            },
            fnRowCallback: function (nRow, aData) {
                $('td:eq(0)', nRow).html(setIconVisibility(aData[0]));
            },
        });
    });

    function setIconVisibility(id) {
        var editString = '<a class="action-icon" style="color:#07078C" href= "javascript:void(0)" onclick="update(' + id + ')"><span class="glyphicon glyphicon-edit" title="Sửa"></span></a>  ';
        var delString = '<a class="action-icon" style="color:#E91E63" href= "javascript:void(0)" onclick="delete(' + id + ')" ><span class="glyphicon glyphicon-remove-circle" title="Xóa"></span></a>';
        return editString + delString;
    }

    function updateProvince(id) {

    }

    function deleteProvince(id) {

    }

</script>
